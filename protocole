connect to server:
	send  connect(name, sha256(sha256(passwd) + time), time)
	server reponse
	if (fail)
		socket is closed
	
create account:
	send create(name, sha256(passwd))
	server response
	if (fail)
		socket is closed
	else
		connected
		

